@model IEnumerable<double>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@foreach (double item in Model)
{
    <a class="btn btn-block @(item == ViewBag.SelectedCategory ? "btn-primary": "btn-outline-secondary")" asp-action="Studiepunten" asp-controller="Vak" asp-route-studiepunten="@item">@item</a>
}
